docker-container: &docker-container
  plugins:
    - docker#v3.8.0:
        image: "public.ecr.aws/automattic/android-build-image:v1.2.0"
        environment:
          - "CI=true"

steps:
  - label: "assemble"
    <<: *docker-container
    command: |
      ./gradlew clean assembleWordpressWasabiDebug -Dscan.tag.exp4
